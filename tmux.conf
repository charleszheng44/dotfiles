# Use the xterm-256color terminal
set -g default-terminal "xterm-256color"
set -ga terminal-overrides "alacritty:Tc"

# customize combination 
set -g prefix C-q
unbind C-b
bind C-q send-prefix

# set vim keybinding
set-window-option -g mode-keys vi 
bind h select-pane -L 
bind j select-pane -D 
bind k select-pane -U 
bind l select-pane -R

# enable mouse
set -g mouse on

# Toggle mouse on with ^Q m
bind m \
  set -g mouse on \;\
  display "Mouse: On"

# Toggle mouse off with ^Q M
bind M \
  set -g mouse off \;\
  display "Mouse: Off"

# source-file $HOME/.tmux/iceberg.tmux.conf
# source-file $HOME/.tmux/gruvbox.tmux.conf
# run-shell "$HOME/.tmux/nord-tmux/nord.tmux"
# source-file "$HOME/.tmux/nordfox.tmux.conf"
# source-file "$HOME/.tmux/mocha.conf"
# source-file "$HOME/.tmux/terafox.tmux.conf"
source-file "$HOME/.tmux/nightfox.tmux.conf"

set -sg escape-time 0

# for linux
# bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'

# for mac
bind-key -T copy-mode-vi y send-keys -X copy-pipe "reattach-to-user-namespace pbcopy"

# set-option -g status-position top

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
