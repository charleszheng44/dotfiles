# Use the xterm-256color terminal
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",*256col*:Tc"

# customize combination 
set -g prefix C-q
unbind C-b
bind C-q send-prefix

# set vim keybinding
set-window-option -g mode-keys vi 
bind h select-pane -L 
bind j select-pane -D 
bind k select-pane -U 
bind l select-pane -R

# enable mouse
set -g mouse on

# Toggle mouse on with ^Q m
bind m \
  set -g mouse on \;\
  display "Mouse: On"

# Toggle mouse off with ^Q M
bind M \
  set -g mouse off \;\
  display "Mouse: Off"

# source-file $HOME/Works/dotfiles/tmux/iceberg.tmux.conf

source-file $HOME/Works/dotfiles/tmux/gruvbox.tmux.conf
set -sg escape-time 0

# for linux
# bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'

# for mac
bind-key -T copy-mode-vi y send-keys -X copy-pipe "reattach-to-user-namespace pbcopy"

set-option -g status-position top

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
# set -g @plugin 'dracula/tmux'
# set -g @dracula-show-powerline true
# set -g @dracula-show-left-icon session
# set -g @dracula-border-contrast true
# set -g @dracula-cpu-usage-label "CPU"
# set -g @dracula-ram-usage-label "RAM"
# set -g @dracula-show-timezone false
# set -g @dracula-show-fahrenheit false
# dracula git related setting
# set -g @dracula-git-show-current-symbol ✓
# set -g @dracula-git-show-diff-symbol !
# set -g @dracula-git-no-repo-message ""
# set -g @dracula-plugins "battery cpu-usage ram-usage time"
# set -g @dracula-plugins "battery time"

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
